type globalParameters = {
  scale: Scales.t
};

type parameters = {
  chance: float,
  chord: array(int),
  filter: float,
  gain: float,
  length: float,
  note: int,
  offset: float,
  pan: float
};
