type globalParameters = {
  scale: Scales.t
};

type parameters = {
  chance: float,
  filter: float,
  gain: float,
  length: float,
  notes: array(int),
  offset: float,
  pan: float,
  transpose: int
};
